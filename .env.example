# Discord bot token (Developer Portal -> Bot -> Reset Token)
DISCORD_TOKEN=

# Application/Client ID (Developer Portal -> General Information -> Application ID)
CLIENT_ID=

# Optional: set this to a guild/server ID while developing so slash commands
# register instantly in that server. Leave blank to register globally
# (global registration can take up to an hour to propagate).
GUILD_ID=
